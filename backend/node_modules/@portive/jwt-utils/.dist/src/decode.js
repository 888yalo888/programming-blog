"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.verifyJWT = exports.parseHeader = void 0;
const jsonwebtoken_1 = __importDefault(require("jsonwebtoken"));
const validate_with_scope_1 = require("./validate-with-scope");
/**
 * Takes a JWT token and parses the header using the Struct.
 *
 * IMPORTANT: It does not verify the JWT token. It only extracts data.
 */
function parseHeader(jwt, struct, scope) {
    const json = jsonwebtoken_1.default.decode(jwt, { complete: true });
    if (json == null)
        throw new Error(`Expected JWT.decode to return a value`);
    return (0, validate_with_scope_1.validateWithScope)(json.header, struct, scope);
}
exports.parseHeader = parseHeader;
/**
 * Takes a JWT token and verifies the payload/body with the `secretKey` and
 * returns the payload/body or throws an error if the token is invalid.
 */
function verifyJWT(jwt, secretKey, struct) {
    const rawPayload = jsonwebtoken_1.default.verify(jwt, secretKey, {
        algorithms: ["HS256"],
        // complete: true,
        clockTolerance: 60, // seconds
    });
    const payload = (0, validate_with_scope_1.validateWithScope)(rawPayload, struct, "JWT token");
    return payload;
}
exports.verifyJWT = verifyJWT;
