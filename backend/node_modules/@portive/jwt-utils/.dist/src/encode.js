"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.signJWT = void 0;
const jsonwebtoken_1 = __importDefault(require("jsonwebtoken"));
const validate_with_scope_1 = require("./validate-with-scope");
/**
 * We provide this `signJWT` method instead of using `JWT.sign` directly to
 * help us verify that the delivered JWT is in the expected shape.
 *
 * We can use the same `headerStruct` and `payloadStruct` in the sender and
 * receiver.
 *
 * This prevents schema errors before they are delivered through the Internet
 * and allows code on the client to fail fast without ever making a connection
 * to the Internet.
 */
function signJWT(payload, payloadStruct, // iat is always added
headerStruct, secretKey, options // expiresIn is an important option here
) {
    const signOptions = Object.assign({ algorithm: "HS256" }, options);
    const jwt = jsonwebtoken_1.default.sign(payload, secretKey, signOptions);
    const complete = jsonwebtoken_1.default.decode(jwt, { complete: true });
    if (complete == null)
        throw new Error(`Expected JWT.decode to return a non-null value`);
    (0, validate_with_scope_1.validateWithScope)(complete.header, headerStruct, "JWT Header");
    (0, validate_with_scope_1.validateWithScope)(complete.payload, payloadStruct, "JWT Payload");
    return jwt;
}
exports.signJWT = signJWT;
